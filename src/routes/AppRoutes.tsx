import {createNativeStackNavigator} from '@react-navigation/native-stack';


const {Navigatior, Screen} = createNativeStackNavigator();

export function AppRoutes(){

    return(
        <Navigatior>
            
        </Navigatior>
    );
}